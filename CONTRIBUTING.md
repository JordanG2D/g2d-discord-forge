# Contributing

Contributions are welcome.

Run tests with:

```bash
PYTHONPATH=src:tests python3 -m unittest discover -s tests -v
```

Never commit a bot token, `.env`, or real deployment JSON. Use synthetic Discord IDs and template codes in tests. Preserve dry-run planning, explicit confirmation codes, permission boundaries, and Forge-only rollback ownership.
